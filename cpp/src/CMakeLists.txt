
add_subdirectory(tests)
add_subdirectory(patterns)

# add the executable
#add_executable(dpcpp ${PROJECT_SOURCE_DIR}/src/dpcpp.cpp)
#target_link_libraries (dpcpp testlib)

# add the install targets
#install (TARGETS dpcpp DESTINATION ${CMAKE_BINARY_DIR}/bin)